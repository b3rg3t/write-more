export enum ERoutes {
  AUTH = "/auth",
  TRIPS = "/",
  PROFILE = "/profile",
  NOTES = "/notes",
  TODOS = "/todos",
  USERS = "/users",
  USER_DETAIL = "/user/:userId",
  TRIP_DETAIL = "/trip/:tripId",
  NOTE_DETAIL = "/note/:noteId",
  TODO_DETAIL = "/todo/:todoId",
}
